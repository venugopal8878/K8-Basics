apiVersion: v1
Kind: pod
metadata:
  name: first pod
  environment: dev
spec:
  containers:
    # docker run -d --name nginx nginx
    - name: nginx
      image: nginx:latest
      port:
        - containerport: 80
